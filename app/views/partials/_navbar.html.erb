<nav role="navigation">
  <div class="nav-wrapper">
    <div class="container">
      <%= link_to 'RoomChecker', rooms_path, class: 'brand-logo' %>
      <% if current_user %>
        <ul id="nav-mobile" class="right">
          <li class="current-user space"><i class="material-icons left">person</i><%= current_user.first_name.upcase %></li>
          <li class="space"><%= link_to 'Home', root_path %></li>
          <% if current_user && current_user.admin? %>
            <li class="space"><%= link_to 'Add room', new_room_path %></li>
          <% end %>
            <li class="space"><%= link_to "Log out", logout_path, method: "delete" %></li>
        </ul>
      <% else %>
        <ul id="nav-mobile" class="right">
          <li><%= link_to 'Login', login_path %></li>
          <li><%= link_to 'Sign up', new_user_path %></li>
        </ul>
      <% end %>
    </div>
  </div>
</nav>

